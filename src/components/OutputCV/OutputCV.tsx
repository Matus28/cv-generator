import styles from "./OutputCV.module.scss";

export const OutputCV = (): JSX.Element => {
  return (
    <div className={styles.wrapper}>
      <div className={styles.document}>CV</div>
    </div>
  );
};
